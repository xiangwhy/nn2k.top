---
import Base from '../layouts/Base.astro';
---

<Base
  title="关于"
  description="关于这个站。"
>
  <article class="container prose about" data-reveal>
    <p class="eyebrow">关于</p>
    <h1>关于</h1>

    <p>
      自从有了 AI，把很多以前没办法实现的想法慢慢做出来了。这个站把其中几个攒下来。
    </p>

    <p>几个口径：</p>
    <ul>
      <li>现在在做什么：side projects — 自管 CMS、嵌入式固件、bash 工具，都在这个站里</li>
      <li>在意什么样的合作：让"想到就做"这件事长期跑下去的环境，比项目规模更重要</li>
      <li>怎么联系：见下面</li>
    </ul>

    <hr />

    <p>
      邮件 <a href="mailto:xiangwhy@gmail.com">xiangwhy@gmail.com</a>，
      看到都回；想合作或者单纯打招呼都行。
    </p>

    <hr />

    <p class="muted">
      站点用 <a href="https://astro.build" rel="noopener noreferrer">Astro</a>
      构建，部署在 <a href="https://pages.cloudflare.com" rel="noopener noreferrer"
        >Cloudflare Pages</a
      >。源码会在你想公开时放到 GitHub。
    </p>
  </article>
</Base>

<style>
  .about {
    padding-block: var(--s-12) var(--s-16);
  }

  .eyebrow {
    font-family: var(--ff-mono);
    font-size: var(--t-xs);
    text-transform: uppercase;
    letter-spacing: 0.12em;
    color: var(--c-muted);
    margin-bottom: var(--s-3);
  }

  .about h1 {
    font-size: clamp(var(--t-3xl), 6vw, var(--t-4xl));
    margin-bottom: var(--s-8);
  }

  .muted {
    color: var(--c-muted);
    font-size: var(--t-sm);
  }
</style>